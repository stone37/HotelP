<section class="container">
    {% if bookings|length > 0 %}
        <div class="row app-user mt-4">
            <div class="col-12">
                <div class="row">
                    {% for booking in bookings %}
                        {{ include('user/booking/_box.html.twig', {'state': false}) }}
                    {% endfor %}
                    <div class="col-12 d-flex justify-content-center mt-2">
                        {{ knp_pagination_render(bookings, 'ui/_pagination.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    {% else %}
        <div class="row app-user d-flex justify-content-center mb-5">
            <div class="col-12 col-md-8 text-center mt-5 mt-md-5">
                <h4 class="h4-responsive font-weight-bold text-dark">
                    Vous n'avez aucune reservation.
                </h4>
                <p>
                    C'est ici que vous verrez toutes vos nouvelles réservations c'est à dire vos reservation non
                    confirmer.
                </p>

                <div class="my-5">
                    <a href="{{ path('app_room_index') }}" class="btn btn-primary px-2 px-md-4">
                        <i class="fas fa-calendar mr-2"></i> Réserver une chambre maintenant
                    </a>
                </div>
            </div>
        </div>
    {% endif %}
</section>


