<section class="row">
    <div class="col-12">
        {{ form_start(booking_form, {'attr': {'novalidate': 'novalidate', 'class': 'admin-form'}}) }}
            <div class="form-row">
                <div class="col-12">
                    {% if (booking_form.lastname is defined) or (booking_form.firstname is defined) or
                        (booking_form.email is defined) or (booking_form.phone is defined) or
                        (booking_form.country is defined) or (booking_form.city is defined) %}
                        <div class="card mb-4">
                            <div class="card-body p-3">
                                <h4 class="h4-responsive font-weight-stone-600 text-dark">Saisissez vos informations</h4>
                                <div class="small-8 info-color text-white px-2 py-1 d-inline-block font-weight-stone-500 mb-1"
                                     style="border-radius: 0.3rem">
                                    Vous y êtes presque ! Complétez simplement les informations requises.
                                </div>

                                <div class="form-row">
                                    {% if booking_form.lastname is defined %}
                                        <div class="col-12 col-md-6 mb-2">
                                            {{ form_row(booking_form.lastname) }}
                                        </div>
                                    {% endif %}

                                    {% if booking_form.firstname is defined %}
                                        <div class="col-12 col-md-6 mb-2">
                                            {{ form_row(booking_form.firstname) }}
                                        </div>
                                    {% endif %}

                                    {% if booking_form.email is defined %}
                                        <div class="col-12 col-md-6">
                                            <small class="form-text text-primary" style="position: absolute; top: -6px;">
                                                L'email de confirmation sera envoyée à cette adresse
                                            </small>
                                            {{ form_row(booking_form.email) }}
                                        </div>
                                    {% endif %}

                                    {% if booking_form.phone is defined %}
                                        <div class="col-12 col-md-6">
                                            <small class="form-text text-primary" style="position: absolute; top: -6px;">
                                                Nous ne vous contacterons qu'en d'urgence
                                            </small>
                                            {{ form_row(booking_form.phone) }}
                                        </div>
                                    {% endif %}

                                    {% if booking_form.country is defined %}
                                        <div class="col-12 col-md-6">
                                            {{ form_widget(booking_form.country) }}
                                        </div>
                                    {% endif %}

                                    {% if booking_form.city is defined %}
                                        <div class="col-12 col-md-6 mb-2">
                                            {{ form_row(booking_form.city) }}
                                        </div>
                                    {% endif %}

                                </div>
                            </div>
                        </div>
                    {% endif %}
                </div>
                <div class="col-12">
                    <div id="booking-form-occupant-wrapper" class="booking-form-occupant-wrapper row"
                         data-prototype="{{ form_widget(booking_form.occupants.vars.prototype)|e('html_attr') }}">
                    </div>
                </div>

                <div class="col-12">
                    <div class="card mb-4">
                        <div class="card-body p-3">
                            <h4 class="h4-responsive font-weight-bold text-dark">Demande spéciales</h4>
                            <div class="small-8">
                                Les demandes spéciales ne peuvent pas être garanties mais nous ferons tout nos possible pour
                                vous satisfaire.
                            </div>
                            <div>
                                {{ form_widget(booking_form.message) }}
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-12">
                    <div class="card mb-3">
                        <div class="card-body">
                            <h4 class="h4-responsive font-weight-bold text-dark">Méthodes de paiement</h4>
                            <div class="small-9 mb-2 dark-grey-text">Nous acceptons les modes de paiement suivants</div>
                            <div class="row">
                                <div class="col-12 col-md-6 app-brand-logo">
                                    <img src="{{ asset('assets/img/brand/mtn.png') }}" class="img-fluid mb-2" data-toggle="tooltip" title="MTN mobile money" alt="MTN mobile money logo">
                                    <img src="{{ asset('assets/img/brand/orange.png') }}" class="img-fluid mx-2 mb-2 orange" data-toggle="tooltip" title="Orange money" alt="Orange money logo">
                                    <img src="{{ asset('assets/img/brand/moov.png') }}" class="img-fluid mx-2 mb-2 moov" data-toggle="tooltip" title="Moov money" alt="Moov money logo">
                                    <img src="{{ asset('assets/img/brand/visa.png') }}" class="img-fluid mx-2 mb-2 visa" data-toggle="tooltip" title="Visa" alt="Visa logo">
                                    <img src="{{ asset('assets/img/brand/masterCard.png') }}" class="img-fluid mx-2 mb-2 mastercard" data-toggle="tooltip" title="Mastercard" alt="Mastercard logo">
                                </div>
                                <div class="col-12 col-md-6">
                                    <div class="border rounded px-2 pt-2 pb-0">
                                        <h6 class="font-weight-stone-600 text-dark">Comptez sur nous</h6>
                                        <div class="">
                                            <ul class="list-unstyled small-8">
                                                <li class="dark-grey-text mb-2"><i class="fas fa-check mx-2  text-success"></i> Nous utilisons une transmission sécurisée</li>
                                                <li class="dark-grey-text"><i class="fas fa-check mx-2 text-success"></i> Nous protégeons vos informations personnelles</li>
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-12 mb-5">
                    <div class="mx-1 mx-md-3 small-9 dark-grey-text mb-3">
                        En cliquant sur "Confirmer et payer maintenant", vous confirmer avoir lu et accepté nos
                        <a href="{{ path('app_cgu') }}" class="font-weight-stone-500 link">conditions d'utilisation</a>,
                        notre <a href="{{ path('app_confidentialite') }}" class="font-weight-stone-500 link">politique de confidentialité</a> et nos
                        <a href="{{ path('app_cgr') }}" class="font-weight-stone-500 link pl-0">conditions de réservation</a>.
                    </div>
                    <div class="text-right">
                        <button type="submit" class="btn btn-primary font-weight-stone-600 mb-3 px-3 px-md-4">
                            <i class="fas fa-lock mr-2"></i> Confirmer et payer maintenant
                        </button>
                    </div>
                </div>
            </div>
            {{ form_widget(booking_form._token) }}
        {{ form_end(booking_form, {'render_rest': false}) }}
    </div>
</section>