{% if rooms|length > 0 %}
    <section class="container home-room blue-grey darken-4 p-lg-4 py-4 px-3 px-md-1">
        <div class="row mx-md-1 px-0 px-md-0">
            <div class="col-12">
                <div class="wow fadeIn">
                    <h1 class="h1-responsive display-4 font-weight-bold text-white text-center mb-3">Nos Hébergements</h1>
                </div>
                <p class="font-weight-stone-500 text-white text-center mb-4">
                    Nos hébergements sont confortable et pratique pour vous, avec des équipements et des services pour répondre à vos besoins.
                </p>
            </div>
            <div class="col-12">
                <div class="text-center">
                    <div class="row">
                        <div id="home-room-carousel" class="carousel slide w-100" data-ride="carousel">
                            <div class="carousel-inner w-100 vv-3" role="listbox">
                                {% for room in rooms %}
                                    <div class="carousel-item {% if loop.first %}active{% endif %}">
                                        <div class="col-12 col-md-4 px-lg-4 px-md-1">
                                            {{ include('site/room/_box.html.twig') }}
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                            <a class="carousel-control-prev controls" href="#home-room-carousel" role="button" data-slide="prev">
                                <i class="fas fa-arrow-left"></i>
                                <span class="sr-only">Previous</span>
                            </a>
                            <a class="carousel-control-next controls" href="#home-room-carousel" role="button" data-slide="next">
                                <i class="fas fa-arrow-right"></i>
                                <span class="sr-only">Next</span>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-12 text-center">
                <a href="{{ path('app_room_index') }}" class="btn btn-primary">
                    Tout afficher <i class="fas fa-angle-double-right ml-2"></i>
                </a>
            </div>
        </div>
    </section>
{% endif %}

